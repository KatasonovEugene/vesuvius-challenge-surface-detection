defaults:
  - model: segresnet
  - metrics: leaderboard
  - datasets: only_val
  - dataloader: default
  - transforms: main
  - tta_transforms: flips
  - _self_
inferencer:
  device_tensors: ["volume"]
  device: auto
  save_path: "submission_dir"
  seed: 1
  from_pretrained: "saved/cutout3d/cutout-normalize-wd1e-5-channels=2.pth"
