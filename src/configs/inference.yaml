defaults:
  - model: segresnet
  - metrics: empty
  - datasets: test
  - dataloader: default
  - transforms: main
  - tta_transforms: empty
  - _self_
inferencer:
  device_tensors: ["volume"]
  device: auto
  save_path: "submission_dir"
  seed: 1
  from_pretrained: "models/cutout-normalize-wd1e-5-channels=2.pth"
