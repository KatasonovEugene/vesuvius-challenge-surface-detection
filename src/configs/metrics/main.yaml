device: "auto"
train:
  - _target_: src.metrics.BinaryDiceMetric
    from_logits: False
    num_classes: 2
    device: ${metrics.device}
    name: "dice"

inference:
  - _target_: src.metrics.BinaryDiceMetric
    from_logits: False
    num_classes: 2
    device: ${metrics.device}
    name: "val_dice"
